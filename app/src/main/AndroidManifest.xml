<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goddoro.memopan">

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_app_token"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_token"
        android:supportsRtl="true"
        android:theme="@style/Theme.MemoPan">
        <activity
            android:name="com.goddoro.memopan.presentation.detail.DetailActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="locale|layoutDirection|screenLayout|screenSize|orientation"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>